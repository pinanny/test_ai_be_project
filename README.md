# test-ai-be-project

A Spring Boot backend (Java 21) used as a small reference service. It exposes REST endpoints to create and look up **resource folders** by name and ID; folder data is kept in memory for the running process.
